# Manan
A simple user registration system

go to command line go to your project directory

if you are on windows run the following command

set FLASK_APP=myapp
flask run

i used cs50 for using sql database.
so install it before running the app

pip install cs50
